# Attic - legacy prototypes

- `array_prototype_legacy.cpp` - original `src/new.cpp` with manual new/delete, int* shape. Kept for history, not built. Replaced by `core/include/dracolix/array.hpp`.
- `gemm_loop_legacy.cpp` - orphan loop fragment, replaced by `core/src/kernels/gemm.cpp/hpp`.
